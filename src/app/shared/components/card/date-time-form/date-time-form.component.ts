/* import { Component, Inject, LOCALE_ID } from '@angular/core';

@Component({
  selector: 'app-date-time-form',
  standalone: true,
})
export class DateTimeFormComponent {
  configure(localeId: string) {
    return {
      component: DateTimeFormComponent,
      providers: [{ provider: LOCALE_ID, useValue: localeId }],
    };
  }
  constructor(@Inject(LOCALE_ID) public localeId: string) {}
}
 */

//Testing component with providers
//imports: [DateTimeFormComponent]
//providers: [{ provide: LOCALE_ID, useValue: 'fr' }],
